-- Runs automatically after migrations on `supabase db reset` (see `config.toml` [db.seed]).
-- Faker-backed fixture data is applied with the Supabase JS client: `npm run db:seed`
-- (needs SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY; local: `supabase status`).

select 1;
